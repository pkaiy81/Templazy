{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "メインウィンドウの基本権限。データ保存はアプリ独自コマンド(load/save)で行う。",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
